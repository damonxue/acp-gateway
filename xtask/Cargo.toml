[package]
name = "xtask"
description = "Build tasks for Agent Gateway, reachable as cargo aliases"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

# Deliberately dependency-free: `cargo dmg` must not trigger a build of
# anything but this file.
[dependencies]
